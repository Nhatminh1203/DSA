#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int b[100005] = {0};

int main(){

    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int n;
    cin >> n;
    int a[n];
    vector<int> v;
    for(int i = 0; i <n; i++){
        cin >> a[i];
        if(b[a[i]] == 0) v.push_back(a[i]);
        b[a[i]] = 1;

    }
    for(int x: v){
        cout << x << " ";
    }
}
